
// tuples

let one := (1, 'one')
print(one)

let other := one
print(other)

let a := 6, b := 'ten', c := True

let some := (a,b,c)
print(some)

var s := some.2
print(s)

some.2 := 'eleven'
print(some)


func test()
  return (2, 'two', 'twelve')
end

var x := test()
print(x)

let p := test()
print(p)
p.3 := 'fifteen'
print(p)

var w := (x=>x^2, x=>x^0.5)

print(w.1(5))    // 25
print(w.2(16))   // 4


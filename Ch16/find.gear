// find in array
use system

extension Array
  func find(item)
    self.forEach(
      func(object)
        if object = item then
          return True
        end
      end)
    return False
  end
end

let a := [1,2,3,4,5,6,7,8,9]

print(a.find(1))

